import { pathToRegexp } from 'path-to-regexp'
import { message } from 'antd'

const idModel = {
  state: {
    
  },
  subscriptions: {
    
  },
  effects: {
    
  },
  reducers: {
    
  },
}

export default idModel
